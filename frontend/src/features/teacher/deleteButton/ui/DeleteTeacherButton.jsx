import DeleteButton from '../../../../shared/components/deleteButton/DeleteButton'

function DeleteTeacherButton({ handleSubmit }) {
    return (
        <DeleteButton handleSubmit={handleSubmit} />
    );
}

export default DeleteTeacherButton;